#!/bin/sh
# Run unit tests before every commit. Fast (<1s), no DB or network needed.
echo "⏳ Running unit tests…"
npm test --silent
if [ $? -ne 0 ]; then
  echo "❌ Unit tests failed — commit aborted. Fix the failures and try again."
  exit 1
fi
echo "✅ All tests passed."
